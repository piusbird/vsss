JOBID=`cat /dev/urandom | tr -dc '0-9' | fold  -w 5 | head -1` # because we lack state
JOBDIR=$HOME/audiorender/current
QUEUEDIR=$HOME/audiorender/queue
ATTICDIR=$HOME/audiorender/attic
CHUNKSIZE=50240 # 10K
VOX="David"
