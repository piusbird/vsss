
JOBID=`cat /dev/urandom | tr -dc '0-9' | fold  -w 5 | head -1` # because we lack state
JOBDIR=/srv/audiorender/current
QUEUEDIR=/srv/audiorender/queue
ATTICDIR=/srv/audiorender/attic
CHUNKSIZE=10240 # 10K

